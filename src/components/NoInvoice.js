import React from "react";
import { Row, Col } from "react-bootstrap";

const NoInvoice = () => {
  return (
    <Row className="show-grid">
      <Col md={12}>No Invoice</Col>
    </Row>
  );
};

export default NoInvoice;
